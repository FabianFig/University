# Programming Assignment 3

Nickle ILOC compiler workspace.

The local Nickle support tree lives in [nickle/](nickle/) and mirrors the upstream Nickle repository at [https://github.com/drz64/nickle](https://github.com/drz64/nickle). The `p1.c` / `p2.c` sources at this level use the shared `nickle.h` runtime when compiling or running the assignment inputs.

Treat generated binaries such as `a.out`, `*.jar`, and similar build outputs as local artifacts only; they do not need to be tracked.
