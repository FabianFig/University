# Assignment 3
Docker Compose example (`composetest/`) with a Flask web service and a Redis counter.

## Contents

- `composetest/`: `compose.yml`, Flask `app.py`, `Dockerfile`, and `requirements.txt`.

## Run / Build

- Run `docker compose up` inside `composetest/` to start the web + Redis services.