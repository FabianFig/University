# Homework 3
Jupyter notebook implementing Stochastic Gradient Descent and an ID3 decision tree from scratch (no ML libraries) for binary classification.

## Highlights

- Stochastic Gradient Descent binary classifier with sigmoid activation trained on `gd-train.dat` and tested on `gd-test.dat`.
- ID3 decision tree trained on `id3-train.dat` and tested on `id3-test.dat`.
- Includes the PDF write-up `CS3793_Assignment3_Output.pdf`.