# Assignment 4
Clustering notebook covering hierarchical (complete-linkage) clustering and k-means with manual distance matrices plus scipy/sklearn verification.

## Highlights

- Complete-linkage hierarchical clustering with manual merge steps and dendrogram.
- k-means clustering from two different centroid initializations.
- Compares manual results against `scipy.cluster.hierarchy` and `sklearn.cluster.KMeans`.