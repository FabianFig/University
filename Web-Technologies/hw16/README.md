# Homework 16
PHP site with MySQL-backed contact storage via `functions.php` database helpers.

## Highlights

- `functions.php`: `dbConnect` helper for the `contact_data` database.
- `contact_query.php` / `query_contacts.php`: query and display stored contacts.