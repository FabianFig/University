# Homework 17
PHP site reading contacts from a MySQL `contact_data` database with shared `functions.php`.

## Highlights

- `functions.php`: database connection helper.
- `query_contacts.php`: lists contacts from the database.
- `index.php` and `navigation.php` for the site shell.