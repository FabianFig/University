# Homework 12
PHP site with server-side form validation helpers in `functions.php` and contact handling in `contact.php`.

## Highlights

- `functions.php`: reusable `isNullOrEmpty`, `validateName`, and `validateEmail` helpers.
- `contact.php`: contact form rendering and validation.